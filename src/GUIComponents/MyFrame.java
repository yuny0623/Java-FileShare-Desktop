package GUIComponents;

import javax.swing.*;

public class MyFrame extends JFrame {
    public MyFrame(){
        setTitle("ImageGhostClient");
        setSize(300, 300);
        setVisible(true);
    }
}
